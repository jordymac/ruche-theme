{{ 'section-brand-story.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="brand-story section-{{ section.id }}-padding gradient color-{{ section.settings.color_scheme }}">
  <div class="brand-story__container page-width">

    {%- comment -%} Problem Section {%- endcomment -%}
    <div class="brand-story__problem">
      {%- if section.settings.problem_subheading != blank -%}
        <p class="brand-story__subheading brand-story__subheading--problem scroll-trigger animate--slide-in" data-cascade style="--animation-order: 1;">
          {{ section.settings.problem_subheading }}
        </p>
      {%- endif -%}

      {%- if section.settings.problem_heading != blank -%}
        <h2 class="brand-story__heading brand-story__heading--problem {{ section.settings.heading_size }} scroll-trigger animate--slide-in" data-cascade style="--animation-order: 2;">
          {{ section.settings.problem_heading }}
        </h2>
      {%- endif -%}

      <div class="brand-story__content-wrapper">
        {%- if section.settings.problem_text != blank -%}
          <div class="brand-story__text brand-story__text--problem rte scroll-trigger animate--slide-in" data-cascade style="--animation-order: 3;">
            {{ section.settings.problem_text }}
          </div>
        {%- endif -%}

        {%- if section.settings.problem_image != blank -%}
          <div class="brand-story__image-wrapper brand-story__image-wrapper--problem">
            {{
              section.settings.problem_image
              | image_url: width: 1200
              | image_tag: class: 'brand-story__image', loading: 'lazy'
            }}
          </div>
        {%- endif -%}
      </div>
    </div>

    {%- comment -%} Solution Section {%- endcomment -%}
    <div class="brand-story__solution">
      {%- if section.settings.solution_image != blank -%}
        <div class="brand-story__image-wrapper brand-story__image-wrapper--solution">
          {{
            section.settings.solution_image
            | image_url: width: 1200
            | image_tag: class: 'brand-story__image', loading: 'lazy'
          }}
        </div>
      {%- endif -%}

      <div class="brand-story__content-wrapper brand-story__content-wrapper--solution">
        {%- if section.settings.solution_subheading != blank -%}
          <p class="brand-story__subheading brand-story__subheading--solution scroll-trigger animate--slide-in" data-cascade style="--animation-order: 1;">
            {{ section.settings.solution_subheading }}
          </p>
        {%- endif -%}

        {%- if section.settings.solution_heading != blank -%}
          <h2 class="brand-story__heading brand-story__heading--solution {{ section.settings.heading_size }} scroll-trigger animate--slide-in" data-cascade style="--animation-order: 2;">
            {{ section.settings.solution_heading }}
          </h2>
        {%- endif -%}

        {%- if section.settings.solution_text != blank -%}
          <div class="brand-story__text brand-story__text--solution rte scroll-trigger animate--slide-in" data-cascade style="--animation-order: 3;">
            {{ section.settings.solution_text }}
          </div>
        {%- endif -%}
      </div>
    </div>

  </div>
</div>

{% schema %}
{
  "name": "Brand Story",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Problem Section"
    },
    {
      "type": "text",
      "id": "problem_subheading",
      "label": "Problem subheading (This Reality font)",
      "default": "The Problem"
    },
    {
      "type": "text",
      "id": "problem_heading",
      "label": "Problem heading",
      "default": "THE PROBLEM"
    },
    {
      "type": "richtext",
      "id": "problem_text",
      "label": "Problem text",
      "default": "<p>Baby products have always been designed with the baby in mind — not the parent. That means clunky prints, unhygienic fabrics, and designs that clash with your lifestyle. Public change tables? A germ trap. Traditional mats? Hard to clean, quick to stain, and anything but chic.</p>"
    },
    {
      "type": "image_picker",
      "id": "problem_image",
      "label": "Problem image (use transparent background for collage effect)"
    },
    {
      "type": "header",
      "content": "Solution Section"
    },
    {
      "type": "text",
      "id": "solution_subheading",
      "label": "Solution subheading (This Reality font)",
      "default": "The Solution"
    },
    {
      "type": "text",
      "id": "solution_heading",
      "label": "Solution heading",
      "default": "OUR SOLUTION"
    },
    {
      "type": "richtext",
      "id": "solution_text",
      "label": "Solution text",
      "default": "<p>Ruche changes the game with parent-first design. Our antibacterial silicone leather mat is naturally germ-resistant, wipes clean in seconds, and folds into a clutch-style silhouette you'll actually want to carry. Safe, vegan, and gentle on sensitive skin — all in elevated tones that blend seamlessly into your wardrobe and your world.</p>"
    },
    {
      "type": "image_picker",
      "id": "solution_image",
      "label": "Solution image"
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "Small"
        },
        {
          "value": "h1",
          "label": "Medium"
        },
        {
          "value": "h0",
          "label": "Large"
        },
        {
          "value": "hxl",
          "label": "Extra large"
        },
        {
          "value": "hxxl",
          "label": "XX-Large"
        }
      ],
      "default": "h0",
      "label": "Heading size"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "Brand Story"
    }
  ]
}
{% endschema %}
